import React from "react";
import Tabs from "./Tabs";
import { metadata } from "@/lib/servicesMeta";

export { metadata };

const page = () => {
  return (
    <div>
      <Tabs/>
    </div>
  );
};

export default page;
